# TCP driver


This repository provides a ROS 2 node to send and receive strings over a tcp port


## Usage
### 1. Launch file
The easiest option to run the driver is using the launch file in `serial_parser` package:
```bash
ros2 ros2 launch tcp_driver tcp_driver.launch
```
